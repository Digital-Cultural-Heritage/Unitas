import 'package:flutter/material.dart';

// ─── Brand Colors (DESIGN.md §1) ─────────────────────────────────────────────
const Color brandNavy  = Color(0xFF1E3A5F);
const Color brandGold  = Color(0xFFC9A84C);
const Color brandGreen = Color(0xFF5B9A6F);
const Color brandBg    = Color(0xFFFDFBF7);

// ─── Surfaces ────────────────────────────────────────────────────────────────
const Color surface = Color(0xFFF5F2EC);
const Color card    = Color(0xFFEDE9E0);

// ─── Text / Dim (navy-based transparency) ────────────────────────────────────
const Color textPrimary  = brandNavy;                          // headings
const Color dim1         = Color(0x99_1E3A5F);                 // 60% navy — body
const Color dim2         = Color(0x61_1E3A5F);                 // 38% navy — muted
const Color dim3         = Color(0x1F_1E3A5F);                 // 12% navy — subtle

// ─── Borders ─────────────────────────────────────────────────────────────────
const Color borderColor  = Color(0x1A_1E3A5F);                 // 10% navy

// ─── Accent shorthands ───────────────────────────────────────────────────────
const Color accentNavy   = brandNavy;
const Color accentGold   = brandGold;

// ─── Nav bar ─────────────────────────────────────────────────────────────────
const Color navBg          = brandNavy;
const Color navIconActive  = brandGold;
const Color navIconInactive = Color(0x8C_FFFFFF);              // white 55%
